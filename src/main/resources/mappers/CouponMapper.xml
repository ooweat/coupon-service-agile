<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.ooweat.mappers.CouponMapper">
  <select id="findAllByCompanySeq" parameterType="kr.co.ooweat.member.domain.Member" resultType="kr.co.ooweat.coupon.domain.CouponConfig">
    SELECT SEQ,
           COMPANY_SEQ,
           ISSUANCE_MAX_COUNT,
           ISSUANCE_MAX_AMOUNT,
           ISSUANCE_MAX_VALIDITY,
           FIXED_USABLE_COUNT,
           FIXED_ISSUANCE_AMOUNT,
           MERCHANT_NAME,
           SEND_COUNT,
           USE_YN,
           CREATE_DATE,
           MODIFY_DATE
    FROM TB_COUPON_CONFIG
    WHERE COMPANY_SEQ = #{companySeq}
  </select>
</mapper>
